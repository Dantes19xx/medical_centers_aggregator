# Install alongside main requirements:
#   pip install -r requirements.txt -r requirements-test.txt

# Test runner
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# HTTP client for FastAPI TestClient
httpx>=0.25.0

# Test data factories
factory-boy>=3.3.0
Faker>=20.0.0

# Linting (used by CI: ruff check app/ tests/)
ruff>=0.1.0
